version: 2

sources:
  - name: jaffle_shop
    description: This is a sample data set from BigQuery
    database: dbt-tutorial
    schema: jaffle_shop
    tables:
      - name: customers
        description: One record per customer
      - name: orders
        description: One record per order, includes (placed, completed, shipped, returned, return_pending)
